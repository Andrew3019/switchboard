You are a measurement subject in a timing experiment. Follow this literally. Your log file is /private/tmp/claude-501/-Users-andrew--herdr-worktrees-switchboard-delivery-experiment/b816b872-d4c3-4d3d-810c-a86640cb1d5e/scratchpad/delivery-logs/SUBJECTNAME.log — create it now with a first line saying the time and that you are starting. Read twenty files from the repo you are working in, one at a time, in this order: PRINCIPLES.md, POC.md, PLAN.md, FEATURES.md, HOOKS.md, BUGS.md, REVIEW.md, QA-FINDINGS.md, BUILD-PLAN.md, braindump.md, switchboard/roles.py, switchboard/live.py, switchboard/models.py, switchboard/output.py, switchboard/validate.py, switchboard/presets.py, switchboard/config.py, switchboard/collector.py, switchboard/board.py, switchboard/panel.py. For each file, in this exact order: run `date +%H:%M:%S` to get the time, read the file, then append ONE line to your log file in the form `<time> file <n>/20 <name> — <one sentence summary>` using the Bash tool with `>>` so it is written immediately and nothing is held in your head, then run `sleep 5` before starting the next file. Do not batch, do not skip the sleeps, do not stop early, do not read anything else, do not run `sb inbox` unless something in your prompt tells you that you have mail. THE IMPORTANT PART: the moment you become aware of ANY message at all, stop what you are doing and immediately append a line to your log file recording, in this order: the current time from `date +%H:%M:%S`, which file number you were on, whether you were mid-step (you had started a file but not yet written its log line) or between steps (you were in or had just finished the five-second sleep), how you became aware of it (either "text appeared in my prompt" or "found by running sb inbox", and say which), and the exact full text of the message. Then carry on with the remaining files exactly as before. If a message tells you to do something, record it that way and keep going with the twenty files anyway unless the message explicitly tells you to stop. When all twenty files are done, append a final line with the time saying you finished, then call `sb done` with a one-line summary. Do not delegate. Do not change any file in the repo.
