#!/usr/bin/env python3
"""MEASUREMENT ONLY. The 'cheap' design's python variant: no switchboard import, no
store, just stamp a per-agent mtime file. Included to separate interpreter-start cost
from switchboard-import cost.
"""
import os
import sys

d = sys.argv[1] if len(sys.argv) > 1 else "/tmp"
name = os.environ.get("HERDR_PANE_ID", "unknown").replace("%", "")
open(os.path.join(d, f"{name}.alive"), "w").close()
raise SystemExit(0)
