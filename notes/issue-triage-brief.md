# issue-triage — task as given

Verbatim from the human:

> delegate a single agent to raed all github issues and figure out if it is still relevant. if not, close it. if so, hand ur findings back to the dispatch.
>
> dispatch: upon receving these, dispatch fixes for each one. do this once u get them, no need to wait. if the process if successful and smooth, u have explicit permissions to create pr and merge, and clean up worktree and agents.

The second paragraph is instruction to the dispatcher, not to you. Your part is the first
paragraph: read all GitHub issues, decide for each whether it is still relevant, close the
ones that are not, and report the still-relevant ones back to your parent (the dispatcher)
in your `sb done` summary — enough per issue that fixes can be dispatched from it.
