<!-- Injected into every spawn this plugin is bound to, flattened to one line and capped at
     [limits] plugin_fragment. Written to fit well under that cap rather than be cut by it.

     It is deliberately ONE bullet, and shorter than either of the other two shipped
     fragments. The whole plan-making instruction is `sb plugin plans guide`, read when the
     job comes up; what travels on every spawn forever is only the sentence that makes an
     agent go and look. Knowing plans exist is not knowing when to make one — an agent told
     only the first infers the second wrongly or not at all — so the trigger states the
     condition ("heading for a change that will land") rather than announcing the plugin.

     It lives here rather than in `protocol.md` on purpose: deleting this folder has to
     take the trigger with it, so that no agent is told plans exist. That is what "off"
     means for this design, and protocol prose survives deletion. -->
# plans

- Is your work heading for a change that will land? Read the plan-making instruction first — `sb plugin plans guide` — and make the plan it tells you to make. Investigation, review, questions and anything you just answer and report need none.
