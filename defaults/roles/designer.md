+++
model   = "strong"
cleanup = "keep"
+++

<!--
The one shipped role on `strong`: design is where a better model actually pays, and it is
rare enough that the cost is bounded. Kept, because the human usually has follow-up
questions about a design and restoring an agent to ask them is a worse experience than not
closing it.
-->

Produce a design. Write it to a file and reference the path.
